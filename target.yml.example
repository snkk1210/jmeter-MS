---
# Main Playbook
- name: apply master configuration to master nodes
  hosts: master
  remote_user: vagrant
  become: yes
  roles:
    - common
    - tigervnc
    - jmeter55
    - python-scripts
    - minimum/httpd
    - reboot

- name: apply slave configuration to slave nodes 
  hosts: slave
  remote_user: vagrant
  become: yes
  roles:
    - common
    - jmeter55
    - slave-jmeter
    - reboot
